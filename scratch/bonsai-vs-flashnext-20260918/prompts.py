# Each: id, category, prompt, check (kind, expected). kinds: exact_num (last number in answer), contains_all, code (tests), rubric (graded by hand)
P = [
 ("m1","math","A tank holds 2,400 litres. Pump A fills it in 40 minutes alone; pump B in 60 minutes alone. Both run for 12 minutes, then A is switched off. How many more minutes does B need to finish filling the tank? Give the number only at the end as 'ANSWER: <n>'.", ("exact_num", 30)),
 ("m2","math","A rectangle's length is 3 more than twice its width. Its perimeter is 48. What is its area? End with 'ANSWER: <n>'.", ("exact_num", 119)),
 ("m3","math","How many trailing zeros does 125! have? End with 'ANSWER: <n>'.", ("exact_num", 31)),
 ("m4","math","A car's odometer reads 12,321 (a palindrome). The driver notices the next palindrome appears exactly 2 hours later. What was the average speed in miles per hour? End with 'ANSWER: <n>'.", ("exact_num", 50)),
 ("m5","math","I have coins totalling $2.55 made of only quarters and dimes, 15 coins in all. How many quarters? End with 'ANSWER: <n>'.", ("exact_num", 7)),
 ("c1","code","Write a Python function `rle(s: str) -> str` that run-length encodes a string: 'aaabccdddd' -> 'a3bc2d4' (a count of 1 is omitted). Return only a ```python code block.", ("code", "assert rle('aaabccdddd')=='a3bc2d4'; assert rle('')==''; assert rle('abc')=='abc'; assert rle('zzzzzzzzzzzz')=='z12'")),
 ("c2","code","Write a Python function `merge_intervals(iv: list[tuple[int,int]]) -> list[tuple[int,int]]` that merges overlapping or touching closed intervals and returns them sorted. Return only a ```python code block.", ("code", "assert merge_intervals([(1,3),(2,6),(8,10),(15,18)])==[(1,6),(8,10),(15,18)]; assert merge_intervals([(1,4),(4,5)])==[(1,5)]; assert merge_intervals([])==[]; assert merge_intervals([(5,6),(1,2)])==[(1,2),(5,6)]")),
 ("c3","code","Write a Python function `top_k_words(text: str, k: int) -> list[str]` returning the k most frequent lowercase words (split on non-letters), ties broken alphabetically. Return only a ```python code block.", ("code", "assert top_k_words('The cat and the hat. The CAT sat!', 2)==['the','cat']; assert top_k_words('b a b a c', 3)==['a','b','c']; assert top_k_words('', 2)==[]")),
 ("c4","code","Write a Python function `parse_duration(s: str) -> int` that converts strings like '1h30m', '45s', '2h', '3m15s', '1h2m3s' into total seconds. Raise ValueError on anything else. Return only a ```python code block.", ("code", "assert parse_duration('1h30m')==5400; assert parse_duration('45s')==45; assert parse_duration('2h')==7200; assert parse_duration('1h2m3s')==3723\ntry:\n    parse_duration('abc'); assert False\nexcept ValueError: pass")),
 ("c5","code","Write a Python function `is_balanced(s: str) -> bool` for brackets ()[]{} ignoring other characters. Return only a ```python code block.", ("code", "assert is_balanced('a(b[c]{d})e'); assert not is_balanced('([)]'); assert is_balanced(''); assert not is_balanced('((')")),
 ("k1","knowledge","Which element has atomic number 74, and what is its chemical symbol? One line.", ("contains_all", ["tungsten","W"])),
 ("k2","knowledge","In what year did the Voyager 1 probe launch, and which planet did it fly past second? One line.", ("contains_all", ["1977","Saturn"])),
 ("k3","knowledge","What does the Linux command `lsblk` list, and what does the `-f` flag add? Two sentences max.", ("rubric", "block devices; -f adds filesystem info (FSTYPE, LABEL, UUID, mountpoint)")),
 ("k4","knowledge","Name the author and the year of first publication of the novel 'The Left Hand of Darkness'. One line.", ("contains_all", ["Le Guin","1969"])),
 ("k5","knowledge","What is the difference between TCP's SYN and FIN flags? Two sentences max.", ("rubric", "SYN initiates/synchronizes a connection (handshake); FIN signals the sender has finished sending, initiating teardown")),
 ("i1","instruction","List exactly five fruits, one per line, each line starting with a dash, all in lowercase, and nothing else in your reply.", ("rubric_format", "5 lines, each '- <lowercase fruit>', nothing else")),
 ("i2","instruction","Write a haiku about a GPU (5-7-5 syllables). Then on a new line write the total syllable count as a number. No other text.", ("rubric", "three lines roughly 5/7/5 then '17'")),
 ("i3","instruction","Rewrite this sentence in the passive voice without changing its meaning, reply with the sentence only: 'The committee approved the new budget on Tuesday.'", ("contains_all", ["was approved by the committee"])),
 ("e1","extraction","From the log lines below, list the unique client IPs that received a 5xx status, one per line, sorted, nothing else.\n\n10.0.0.5 GET /api 200\n10.0.0.9 POST /login 502\n10.0.0.5 GET /api 503\n10.0.0.7 GET / 404\n10.0.0.9 GET /x 500\n10.0.0.2 GET /health 200\n10.0.0.11 PUT /cfg 504", ("rubric_format", "10.0.0.11, 10.0.0.5, 10.0.0.9 each on a line (string sort) and nothing else")),
 ("e2","extraction","Summarize the following in one sentence of at most 25 words:\n\nThe R9700 is a 32 GB RDNA4 workstation card. Six of them in one Threadripper box give 192 GB of VRAM. A 26B mixture-of-experts model in FP8 fits on two cards with room for cache, so three replicas run at once. The best serving layout kept attention data-parallel across the pair and split the experts, avoiding the per-layer all-reduce that tensor parallelism pays on PCIe.", ("rubric", "<=25 words, captures 6x R9700 / 3 replicas / DP+EP layout beating TP")),
 ("l1","logic","Alice, Bob and Carol each have a different pet: a cat, a dog, a fish. Alice is allergic to fur. Bob does not have the dog. Who has which pet? End with 'ANSWER: Alice=<pet>, Bob=<pet>, Carol=<pet>'.", ("contains_all", ["Alice=fish","Bob=cat","Carol=dog"])),
 ("l2","logic","If all bloops are razzies and some razzies are lazzies, must some bloops be lazzies? Answer 'yes' or 'no' first, then one sentence of justification.", ("rubric", "no (some razzies being lazzies need not include any bloop)")),
]
